# MDPI Data Availability Text

Use this version once the GitHub repository is public:

```text
The processor-in-the-loop validation evidence supporting the embedded
controller-execution claims is available in a public GitHub repository at
[repository URL]. The repository includes PIL documentation, paired
host-reference and C2000 serial logs, command-error summaries, generated figures,
metadata, and SHA-256 file hashes. The PIL validation executes the WF-RS
controller step on a TI LAUNCHXL-F28379D C2000 target while the sailing plant and
environment remain simulated on the host computer; it is not an on-water field
trial.
```

Use this version if the repository is private before publication:

```text
The processor-in-the-loop validation evidence supporting the embedded
controller-execution claims will be made available in a public repository upon
publication.
```

Repository description for GitHub:

```text
Processor-in-the-loop evidence for WF-RS autonomous sailing controller execution on a TI C2000 target.
```
